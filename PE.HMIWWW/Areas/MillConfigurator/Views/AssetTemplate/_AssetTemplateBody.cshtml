@using Kendo.Mvc.UI
@using PE.HMIWWW.Core.Extensions;
@using PE.HMIWWW.Core.Resources
@model PE.HMIWWW.Areas.MillConfigurator.ViewModels.AssetTemplate.VM_AssetTemplate

<div class="row h-100">
  <div class="col">
    @(Html.Kendo().TabStrip()
    .Name("AssetTemplateBody")
    .Animation(false)
    .HtmlAttributes(new {style = "width: 100%;"})
    .Scrollable(false)
    .Items(async tabstrip =>
    {
    tabstrip.Add().Text(VM_Resources.NAME_AssetTemplate)
    .Selected(true).Content((await Html.PartialAsync("~/Areas/MillConfigurator/Views/AssetTemplate/_AssetTemplateDetails.cshtml", Model)).ToHtmlString());
    })
    )
  </div>
</div>
