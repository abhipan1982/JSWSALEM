@model PE.HMIWWW.ViewModel.Module.Lite.Defect.VM_DefectCatalogueCategory

<div class="row h-100">
    <div class="col">
        @(Html.Kendo().TabStrip()
            .Name("DefectCatalogueCategoriesBody")
            .Animation(false)
            .Scrollable(false)
            .Items(tabstrip =>
            {
                tabstrip.Add().Text(VM_Resources.NAME_DefectsCatalogueCategoriesDetails)
                    .Selected(true).Content(Html.Partial("~/Views/Module/PE.Lite/DefectCatalogueCategories/_DefectCatalogueCategoriesDetails.cshtml", Model).ToHtmlString()).ContentHtmlAttributes(new {@class = "partialHeight"});
            })
            .Events(e => { e.Select("saveTabStripState"); })
            )
    </div>
</div>