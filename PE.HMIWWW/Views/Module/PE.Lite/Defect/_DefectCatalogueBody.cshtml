@model PE.HMIWWW.ViewModel.Module.Lite.Defect.VM_DefectCatalogue

<div class="row h-100">
    <div class="col">
        @(Html.Kendo().TabStrip()
            .Name("DefectCatalogueBody")
            .Animation(false)
            .Scrollable(false)
            .Items(tabstrip =>
            {
                tabstrip.Add().Text(VM_Resources.NAME_DefectCatalogueDetails)
                    .Selected(true).Content(Html.Partial("~/Views/Module/PE.Lite/Defect/_DefectCatalogueDetails.cshtml", Model).ToHtmlString()).ContentHtmlAttributes(new {@class = "partialHeight"});
            })
            .Events(e => { e.Select("saveTabStripState"); })
            )
    </div>
</div>