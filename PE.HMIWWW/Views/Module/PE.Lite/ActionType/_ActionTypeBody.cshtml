@model PE.HMIWWW.ViewModel.Module.Lite.Maintenance.VM_ActionType

<div class="row h-100">
    <div class="col">
        @(Html.Kendo().TabStrip()
            .Name("ActionTypeBody_" + Guid.NewGuid())
            .Animation(false)
            .Scrollable(false)
            .Items(tabstrip =>
            {
                tabstrip.Add().Text(VM_Resources.NAME_Details)
                    .Selected(true).Content(Html.Partial("~/Views/Module/PE.Lite/ActionType/_ActionTypeDetails.cshtml", Model).ToHtmlString()).ContentHtmlAttributes(new {@class = "partialHeight"});
            })
            .Events(e => { e.Select("saveTabStripState"); }))
    </div>
</div>