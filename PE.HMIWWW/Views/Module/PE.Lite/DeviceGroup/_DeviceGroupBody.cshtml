@model PE.HMIWWW.ViewModel.Module.Lite.Maintenance.VM_DeviceGroup

<div class="row h-100">
    <div class="col">
        @(Html.Kendo().TabStrip()
            .Name("DeviceGroupBody")
            .Animation(false)
            .Scrollable(false)
            .Items(tabstrip =>
            {
                tabstrip.Add().Text(VM_Resources.NAME_Details)
                    .Selected(true).Content(Html.Partial("~/Views/Module/PE.Lite/DeviceGroup/_DeviceGroupDetails.cshtml", Model).ToHtmlString()).ContentHtmlAttributes(new {@class = "partialHeight"});
            })
            .Events(e => { e.Select("saveTabStripState"); })
            )
    </div>
</div>